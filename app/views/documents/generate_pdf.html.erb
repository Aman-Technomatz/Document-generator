<!DOCTYPE html>
<html>
<head>
  <title>Experience Letter</title>
  <style>

  body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    font-size: 12pt;
    height: 100%;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }


  h1, h2, h3 {
    text-align: center;
  }

  .disclaimer {
    text-align: center;
    font-weight: bold;
    font-size: 14px;
  }

  .header {
    text-align: center;
    font-weight: bold;
    font-size: 18px;
    # position: absolute;
    top: 0;
    left: 0;
    right: 0;
    padding: 0mm 0;
    margin: 0;
  }

  footer {
    text-align: center;
    font-size: 12px;
    padding: 5mm 0;
    margin-top: 1000px;
  }

  .content_offer {
    padding: 17px;
    flex: 1;
  }

  .content_experience {
    padding: 77px;
    flex: 1;
  }

  .content_appointment {
    padding: 20px;
    flex: 1;
  }

  .content_relieving {
    padding: 157px;
    flex: 1;
  }

  </style>
</head>
<body>

  <div class="header">
    <%= render 'header' %>
  </div>

  <% if @document.document_type == 'experience_letter' %>
    <%= render 'experience_letter' %>
  <% elsif @document.document_type == 'relieving_letter' %>
    <%= render 'relieving_letter' %>
  <% elsif @document.document_type == 'appointment_letter' %>
    <%= render 'appointment_letter' %>
  <% elsif @document.document_type == 'offer_letter' %>
    <%= render 'offer_letter' %>
  <% end %>

  <div class="footer">
    <%= render 'footer' %>
  </div>

</body>
</html>
<%# unless request.format.pdf? %>
  <%#= link_to "Download PDF", document_path(@document, format: :pdf), class: "btn btn-primary" %>
<%# end %>
