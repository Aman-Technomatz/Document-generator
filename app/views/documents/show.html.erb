<!DOCTYPE html>
<html>
<head>
  <title>Experience Letter</title>
  <style>
    body { font-family: Arial, sans-serif; color: #333; padding: 20px; }
    .header, .footer { text-align: center; font-size: 12px; }
    h2, h3 { text-align: center; }
    .content { margin-top: 30px; line-height: 1.6; }
    .disclaimer { font-size: 10px; color: #666; margin-top: 20px; text-align: center; }
    hr { border: 1px solid #333; }
  </style>
</head>
<body>

  <!-- Header Section -->
  <%= render 'header' %>

  <!-- Content Section -->
  <% if @document.document_type == 'Experience Letter' %>
    <div class="content">
      <%= render 'experience_letter' %>
    </div>
  <% elsif @document.document_type == 'Resign Letter' %>
    <%= render '#' %>
  <% elsif @document.document_type == 'Increment Letter' %>
    <%= render '#' %>
  <% elsif @document.document_type == 'Apprication Letter' %>
    <%= render '#' %>
  <% end %>

  <!-- Footer Section -->
  <%= render 'footer' %>

</body>
</html>
<% unless request.format.pdf? %>
  <%= link_to "Download PDF", document_path(@document, format: :pdf), class: "btn btn-primary" %>
<% end %>
